
#include <stdio.h>
#include <string.h>

int main() {
    int num;
    printf("Ingrese un número entero positivo para encontrar su factorial: ");
    scanf("%d", &num);
        while(1){
            printf("\n", num);

        }
    return 0;
}
